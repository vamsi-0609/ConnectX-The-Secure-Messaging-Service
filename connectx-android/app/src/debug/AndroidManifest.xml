<?xml version="1.0" encoding="utf-8"?>
<!--
     N3.5: debug-variant manifest override, merges additively into
     app/src/main/AndroidManifest.xml for debug builds only (no tools:replace
     needed; the main manifest does not already declare
     android:networkSecurityConfig, so there is no merge conflict). Points at
     network_security_config_debug.xml, which permits cleartext traffic to
     exactly 10.0.2.2 (the debug BASE_URL's host) and nothing else. Release
     builds never see this file.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:networkSecurityConfig="@xml/network_security_config_debug" />

</manifest>
